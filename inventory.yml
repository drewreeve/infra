---
all:
  hosts:
    cherrypi:
      ansible_user: "pi"
      ansible_host: "10.23.1.15"
    loki:
      ansible_user: "ubuntu"
      ansible_host: "10.23.1.20"
  children:
    dns:
      hosts:
        # primary
        loki:
        # secondary
        cherrypi:
